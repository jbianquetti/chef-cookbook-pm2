#!/bin/bash

cd <%= @path %>/current

npm -g install

pm2 stop <%= @name %>
pm2 kill
PORT=<%= @port %> pm2 start <%= @js %> \
    -n <%= @name %> \
    --run-as-user <%= @user %> \
    --run-as-group <%= @group %> \
    -i max

